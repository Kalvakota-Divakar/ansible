- name : loops example
  hosts: local
  connection: local
  tasks:
  - name: print names
    ansible.builtin.debug:
      msg: "hello {{ NAME }}"
    loop:
    - ramesh
    - divakar
    - suresh
    - srinivas