- name: installing packages using loop
  hosts: local
  connection: local
  become: true
  tasks:
  - name: install package
    ansible.builtin.package:
      name: "{{ item}}"
      state: present
    loop:
    - mysql
    - nginx
    - zip


